// Question 5
// Difference between the ?? and ? is that ?? is used when we check that if the variable is null than assign it the value. On the
// other side single ? is used to make the varible default value null without assigning it a value.